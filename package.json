{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build && vite build --ssr && vite build && vite build --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr && vite build && vite build --ssr && vite build && vite build --ssr --ssr --ssr --ssr --ssr",
        "clear-cache": "vite clear-cache"
    },
    "devDependencies": {
        "@headlessui/react": "^1.7.13",
        "@inertiajs/react": "^1.0.0",
        "@tailwindcss/forms": "^0.5.3",
        "@vitejs/plugin-react": "^3.0.0",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.12",
        "axios": "^1.3.2",
        "dotenv": "^16.0.3",
        "laravel-vite-plugin": "^0.7.4",
        "lodash": "^4.17.19",
        "postcss": "^8.4.18",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "tailwindcss": "^3.2.1",
        "vite": "^4.1.1"
    },
    "dependencies": {
        "@arcgis/core": "^4.25.5",
        "@fortawesome/fontawesome-svg-core": "^6.3.0",
        "@fortawesome/free-regular-svg-icons": "^6.3.0",
        "@fortawesome/free-solid-svg-icons": "^6.3.0",
        "@fortawesome/react-fontawesome": "^0.2.0",
        "@inertiajs/react": "^1.0.0",
        "@vitejs/plugin-react-refresh": "^1.3.6",
        "antd": "^5.3.2",
        "daisyui": "^2.50.0",
        "gsap": "^3.11.4",
        "laravel-echo": "^1.15.0",
        "moment": "^2.29.4",
        "nprogress": "^0.2.0",
        "prop-types": "^15.8.1",
        "pusher-js": "^8.0.2",
        "react": "^18.2.0",
        "react-confetti": "^6.1.0",
        "react-dom": "^18.2.0",
        "react-icons": "^4.4.0",
        "react-is": "^16.8.0",
        "react-portal": "^4.2.2",
        "react-router-dom": "^6.10.0",
        "react-toastify": "^9.1.2",
        "sweetalert2": "^11.7.1",
        "tailwind-datepicker-react": "^1.2.3",
        "vanilla-tilt": "^1.8.0",
        "vite-plugin-dynamic-import": "^1.2.7",
        "vite-plugin-env-compatible": "^1.1.1"
    }
}
